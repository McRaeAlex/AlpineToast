{
  "name": "alpine-toast",
  "version": "1.0.0",
  "description": "A toast library using alpine-js for phoenix-liveview",
  "main": "src/index.js",
  "scripts": {
    "build": "rollup --config",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^2.39.0"
  },
  "dependencies": {
    "typescript": "^4.1.5"
  }
}
